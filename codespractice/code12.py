#WAP to calculate the value of n + nn + nnn, where n is an integer input by the user.

#monthly calender display
import calendar
#take user input
y= int(input("Input the year: "))
m= int (input("Input the month:"))
print(calendar.month(y,m))
